# Copy this file to .env and replace the placeholder before starting Docker Compose.
CLAIMS_DB_NAME=claims
CLAIMS_DB_USERNAME=claims_app
CLAIMS_DB_PASSWORD=replace-with-a-local-password
RABBITMQ_USERNAME=claims_app
RABBITMQ_PASSWORD=replace-with-another-local-password

# Claims API bearer-token validation. These defaults target the local identity
# provider that will be added in the next authentication story.
CLAIMS_JWT_ISSUER_URI=http://localhost:8090/realms/insurance-platform
CLAIMS_JWT_JWK_SET_URI=http://localhost:8090/realms/insurance-platform/protocol/openid-connect/certs
CLAIMS_JWT_AUDIENCE=claims-service
CLAIMS_CORS_ALLOWED_ORIGINS=http://localhost:5173

# Local identity provider. Use unique local-only passwords; these values are read
# by Docker Compose and substituted into the imported Keycloak realm.
KEYCLOAK_ADMIN_USERNAME=admin
KEYCLOAK_ADMIN_PASSWORD=replace-with-a-local-admin-password
KEYCLOAK_AGENT_USERNAME=claims-agent
KEYCLOAK_AGENT_PASSWORD=replace-with-a-local-agent-password
KEYCLOAK_REVIEWER_USERNAME=claims-reviewer
KEYCLOAK_REVIEWER_PASSWORD=replace-with-a-local-reviewer-password
KEYCLOAK_ADMIN_USER_USERNAME=claims-admin
KEYCLOAK_ADMIN_USER_PASSWORD=replace-with-a-local-platform-admin-password

# Optional: uncomment only when running the claim-summary worker with its OpenAI provider.
# OPENAI_API_KEY=replace-with-your-openai-api-key
